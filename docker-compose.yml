services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_RAZORPAY_KEY_ID: ${NEXT_PUBLIC_RAZORPAY_KEY_ID}
    container_name: epsilontechnext_app
    restart: unless-stopped
    ports:
      - "3002:3000"
    env_file:
      - .env
